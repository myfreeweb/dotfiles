#!/bin/sh

OS="$(uname -s)"

case "$OS" in
	Darwin)  diskutil erasevolume HFS+ 'ramdisk' "$(hdiutil attach -nomount ram://4194304)";;
	Linux)   mount -t ramfs -o size=1g ramfs /mnt/ramdisk;;
	FreeBSD) mount_mfs -s 2097152 md /mnt/ramdisk;;
	*)       echo "Unknown OS";;
esac

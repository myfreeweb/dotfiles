#!/usr/bin/env zsh

if [[ -e $commands[bsdinfo] ]]; then; bsdinfo
elif [[ -e $commands[screenfetch-dev] ]]; then; screenfetch-dev
elif [[ -e $commands[screenfetch] ]]; then; screenfetch
elif [[ -e $commands[archey] ]]; then; archey -c
fi
